---
description: Bookmark this session in Agent Session Bookmark as one to return to after a restart (optional note)
argument-hint: [note]
allowed-tools: Bash(__ASB_CLI__:*)
---
Flag result: !`__ASB_CLI__ add --current $ARGUMENTS`

This session has just been bookmarked in Agent Session Bookmark's "Return to" list (the result line above says so, or explains why it could not be). Reply with ONE short line: confirm it is bookmarked, echo the note if one was given, and mention I can leave with /exit whenever I'm ready. If the result line reports an error, quote it instead. Do nothing else.
